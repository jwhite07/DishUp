# require 'rails_helper'
#
# RSpec.describe DishRating, type: :model do
#   before {@dish_rating = FactoryGirl.build(:dish_rating)}
#   subject {@dish_rating}
#
#   it {should respond_to(:dish_id)}
#   it {should respond_to(:user_id)}
#   it {should be_valid}
#   it {should validate_presence_of(:dish_id)}
#   it {should validate_presence_of(:user_id)}
#   it {should validate_presence_of(:rating)}
#   it {should allow_value(rand(100...500) * 1.0 / 100).for(:rating)}
# end
